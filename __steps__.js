/**
 * create separate routes component and export 
 * create separate components 
 * use react botstarap 
 * use momment.js for time 
 * -npm install moment
 * -import moment from 'moment';
 * use react-marquee
 * */